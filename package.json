{
  "name": "stike-extension",
  "displayName": "Stike Extension",
  "description": "VS Code extension for Stike",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "stike.reviewCode",
        "title": "Review and Fix Code",
        "category": "Stike"
      },
      {
        "command": "stike.openChatbot",
        "title": "Open Chatbot",
        "category": "Stike",
        "icon": "$(comment-discussion)"
      }
    ],
    "configuration": {
      "title": "Stike Code Reviewer",
      "properties": {
        "stike.apiKey": {
          "type": "string",
          "default": "",
          "description": "Google GenAI API Key (or set GOOGLE_GENAI_API_KEY environment variable)",
          "scope": "application"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "@typescript-eslint/parser": "^5.45.0",
    "eslint": "^8.28.0",
    "typescript": "^4.9.4"
  }
}